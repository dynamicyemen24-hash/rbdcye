    <section className="relative bg-[#052317] pt-28 lg:pt-40 pb-24 lg:pb-32 overflow-hidden" style={{ direction: "rtl" }}>
      
      {/* Subtle overlay texture */}
      <div className="absolute inset-0 pattern-bg-gold opacity-5 pointer-events-none z-0" />
      <div className="absolute -top-[20%] -right-[10%] w-[60%] h-[60%] bg-[var(--brand-green)]/20 blur-[140px] rounded-full pointer-events-none z-0" />

      {/* Content wrapper */}
      <div className="relative z-10 w-full">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          
          <div className="grid lg:grid-cols-12 gap-12 lg:gap-16 items-start">
            
            {/* ───── Right Column (Arabic layout, visual weight leading) ───── */}
            <div className="lg:col-span-6 xl:col-span-7 text-right">
              <motion.div
                initial={{ opacity: 0, y: 30 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.8, ease: [0.16, 1, 0.3, 1] }}
              >
                {/* Micro-credential trust badge */}
                <div className="inline-flex items-center gap-3 bg-white/5 backdrop-blur-md border border-white/10 rounded-full px-5 py-2.5 mb-8 shadow-sm hover:bg-white/10 transition-colors">
                  <Shield className="w-4.5 h-4.5 text-[var(--brand-gold-light)] shrink-0" />
                  <span className="text-white/90 text-xs sm:text-sm font-semibold leading-normal font-readex">
                    إشراف استشاري وتنموي • حوكمة معتمدة وإجراءات رقابة ميدانية
                  </span>
                </div>

                {/* Singular, powerful dominant headline emphasizing human dignity and long-term impact */}
                <h1 className="text-white mb-6 font-alexandria font-black tracking-tight" style={{ fontSize: "clamp(2rem, 4.2vw, 3.75rem)", lineHeight: 1.3 }}>
                  نصنع <span className="text-[var(--brand-gold-light)] font-extrabold">التنمية المستدامة</span>
                  <br />
                  ونحفظ <span className="relative inline-block text-[var(--brand-gold-light)] font-extrabold">الكرامة الإنسانية
                    <span className="absolute bottom-2 left-0 right-0 h-1.5 bg-[var(--brand-gold)] rounded-full opacity-50" />
                  </span>
                </h1>

                {/* Highly communicative, professional subtitle avoiding generic slogans */}
                <p className="text-white/80 mb-10 lg:mb-14 max-w-2xl font-readex leading-relaxed" style={{ fontSize: "clamp(0.95rem, 1.2vw, 1.15rem)", fontWeight: 300 }}>
                  مؤسسة وطنية رائدة متخصصة في هندسة الأثر وتطبيق حلول التنمية المستدامة باليمن. نتبنى نماذج حوكمة مرنة في إدارة المساعدات وإطلاق مشاريع السقيا النظيفة، التمكين الاقتصادي للأسر المنتجة، والتدخلات الإغاثية المستندة إلى المسوح الميدانية الديموغرافية والتوثيق الرقمي الكامل.
                </p>

                {/* Premium Glassmorphism CTA Cards */}
                <div className="flex flex-col sm:flex-row items-stretch sm:items-center justify-start gap-4 sm:gap-5 mb-14 lg:mb-20">
                  <motion.button
                    onClick={() => setCurrentPage("donate")}
                    className="w-full sm:w-auto group relative flex items-center justify-center gap-3 px-8 py-4 bg-gradient-to-br from-[var(--brand-gold)]/20 to-[var(--brand-gold)]/5 backdrop-blur-xl border border-[var(--brand-gold)]/30 text-white rounded-2xl font-extrabold shadow-[0_8px_32px_rgba(0,0,0,0.2)] hover:shadow-[0_16px_48px_rgba(212,175,55,0.25)] hover:border-[var(--brand-gold)]/60 hover:from-[var(--brand-gold)]/30 hover:to-[var(--brand-gold)]/10 transition-all duration-300 text-base lg:text-lg focus:outline-none focus:ring-2 focus:ring-[var(--brand-gold)] focus:ring-offset-4 focus:ring-offset-[#052317] font-readex overflow-hidden"
                    whileHover={{ y: -2 }}
                    whileTap={{ scale: 0.98 }}
                  >
                    {/* Subtle internal highlight */}
                    <div className="absolute inset-0 bg-gradient-to-t from-transparent to-white/10 opacity-50" />
                    <Heart className="relative z-10 w-5 h-5 text-[var(--brand-gold-light)] fill-[var(--brand-gold-light)] drop-shadow-md" />
                    <span className="relative z-10 drop-shadow-sm">المساهمة وبوابة الزكاة</span>
                  </motion.button>

                  <motion.button
                    onClick={() => setCurrentPage("programs")}
                    className="w-full sm:w-auto group flex items-center justify-center gap-3 px-7 py-4 bg-white/5 backdrop-blur-xl text-white/90 border border-white/10 rounded-2xl shadow-[0_8px_32px_rgba(0,0,0,0.15)] hover:bg-white/10 hover:text-white hover:border-white/25 transition-all duration-300 font-semibold text-sm lg:text-base focus:outline-none focus:ring-2 focus:ring-white/50 focus:ring-offset-4 focus:ring-offset-[#052317] font-readex relative overflow-hidden"
                    whileHover={{ y: -2 }}
                    whileTap={{ scale: 0.98 }}
                  >
                    {/* Subtle internal highlight */}
                    <div className="absolute inset-0 bg-gradient-to-t from-transparent to-white/5 opacity-50" />
                    <span className="relative z-10">مصفوفة البرامج التنموية</span>
                  </motion.button>
                </div>

                {/* Highly detailed Trust Metric Blocks instead of generic stat grids */}
                <div className="border-t border-white/10 pt-10 max-w-2xl">
                  <div className="grid grid-cols-3 gap-4 md:gap-6">
                    {[
                      { value: formatStat(metrics?.totalBeneficiaries), label: "مستفيد ميداني موثق", icon: Users },
                      { value: formatStat(metrics?.activeProjects), label: "مشروع تنموي مستدام", icon: Star },
                      { value: formatStat(metrics?.totalPartners), label: "مانح وشريك استراتيجي", icon: Handshake },
                    ].map((stat, idx) => {
                      const Icon = stat.icon;
                      return (
                        <div key={idx} className="text-right">
                          <div className="flex items-center gap-2 mb-2">
                            <Icon className="w-4 h-4 text-[var(--brand-gold-light)] shrink-0 opacity-90" />
                            <span className="text-xs font-semibold text-white/60 font-readex">{stat.label}</span>
                          </div>
                          <div className="text-white font-extrabold font-alexandria text-xl md:text-2xl tracking-tight">
                            {stat.value}
                          </div>
                        </div>
                      );
                    })}
                  </div>
                </div>

              </motion.div>
            </div>

            {/* ───── Left Column (Majestic Spiritual Covenant and Trust Panel) ───── */}
            <div className="lg:col-span-6 xl:col-span-5 mt-16 lg:mt-0">
              <motion.div
                initial={{ opacity: 0, scale: 0.95 }}
                animate={{ opacity: 1, scale: 1 }}
                transition={{ duration: 0.8, delay: 0.2, ease: [0.16, 1, 0.3, 1] }}
                onMouseEnter={() => setIsPaused(true)}
                onMouseLeave={() => setIsPaused(false)}
                className="relative rounded-[2rem] p-8 sm:p-10 lg:p-12 overflow-hidden bg-gradient-to-br from-white/10 to-white/5 backdrop-blur-2xl border border-white/15 shadow-[0_20px_60px_rgba(0,0,0,0.3)] min-h-[420px] flex flex-col justify-between group"
              >
                {/* Soft gold radial flare */}
                <div className="absolute -top-20 -left-20 w-64 h-64 rounded-full opacity-20 pointer-events-none transition-opacity duration-700 group-hover:opacity-30" style={{ background: "radial-gradient(circle, var(--brand-gold), transparent 70%)" }} />

                {/* Section B: Spiritual Guidance (Rotating Quranic Ayah/Hadith Widget) */}
                <div className="relative h-full flex flex-col justify-between flex-1">
                  <div>
                    <div className="flex flex-wrap items-center justify-between gap-4 mb-10 pb-6 border-b border-white/10">
                      <span className="px-4 py-2 rounded-full bg-[var(--brand-gold)]/15 backdrop-blur-md border border-[var(--brand-gold)]/25 text-[var(--brand-gold-light)] text-xs sm:text-sm font-bold flex items-center gap-2 font-readex shadow-sm">
                        <Sparkles className="w-4 h-4 text-[var(--brand-gold-light)]" />
                        {current.type === 'ayah' ? 'تذكير بالآية الكريمة' : 'تذكير بالحديث الشريف'}
                      </span>
                      <span className="text-white/70 text-sm font-medium font-readex bg-black/20 px-3 py-1.5 rounded-lg border border-white/5 shadow-inner">{current.reference}</span>
                    </div>

                    <div className="min-h-[160px] flex items-center mb-6 overflow-hidden">
                      <AnimatePresence mode="wait">
                        <motion.div
                          key={verseIndex}
                          initial={{ opacity: 0, y: 15 }}
                          animate={{ opacity: 1, y: 0 }}
                          exit={{ opacity: 0, y: -15 }}
                          transition={{ duration: 0.5, ease: "easeInOut" }}
                          className="w-full py-2"
                        >
                          <p className="text-white font-medium text-lg sm:text-xl lg:text-2xl leading-relaxed text-right font-serif tracking-wide" style={{
                            fontFamily: "'Noto Naskh Arabic', 'Traditional Arabic', serif",
                            lineHeight: 2.1,
                            textShadow: "0 4px 12px rgba(0,0,0,0.25)"
                          }}>
                            {current.arabic}
                          </p>
                        </motion.div>
                      </AnimatePresence>
                    </div>
                  </div>

                  <div className="mt-6 pt-6 border-t border-white/10 flex flex-col sm:flex-row items-center justify-between gap-6">
                    {/* Manual control slider dots with larger touch targets for accessibility */}
                    <div className="flex gap-2" dir="ltr" role="tablist">
                      {ISLAMIC_TEXTS.map((_, i) => (
                        <button
                          key={i}
                          role="tab"
                          aria-selected={i === verseIndex}
                          aria-label={`تغيير النص ${i + 1}`}
                          onClick={() => setVerseIndex(i)}
                          className="relative group p-2 -m-2 focus:outline-none"
                        >
                          <span className={`block h-2 rounded-full transition-all duration-500 ease-out ${i === verseIndex ? 'w-10 bg-[var(--brand-gold)] shadow-[0_0_12px_rgba(212,175,55,0.6)]' : 'w-2.5 bg-white/20 group-hover:bg-white/40 group-focus:ring-2 ring-white/50'}`} />
                        </button>
                      ))}
                    </div>
                    
                    <span className="text-white/40 text-[11px] sm:text-xs font-readex tracking-wide">مؤسسة رحماء بينهم للأعمال الإنسانية والتنموية</span>
                  </div>
                </div>

              </motion.div>
            </div>

          </div>

          {/* Majestic Breathing Room (≈ 5cm on desktop) between Text/Stats and Video */}
          <div className="mt-16 lg:mt-28 w-full mx-auto relative rounded-3xl lg:rounded-[2.5rem] overflow-hidden shadow-[0_30px_80px_rgba(0,0,0,0.4)] border border-white/10 aspect-video md:aspect-[21/9] bg-black/50">
             <HeroVideoBlock />
          </div>

          {/* Breathing room after video and scroll indicator */}
          <div className="mt-12 flex justify-center pb-8">
            <button 
              type="button"
              className="group flex flex-col items-center gap-3 text-white/50 hover:text-white/90 transition-all focus:outline-none focus:ring-2 focus:ring-white/30 rounded-lg p-2" 
              onClick={() => {
                const target = document.getElementById('quick-donation');
                if (target) target.scrollIntoView({ behavior: 'smooth' });
              }}
            >
              <span className="text-[0.65rem] tracking-wider uppercase font-semibold font-readex group-hover:-translate-y-1 transition-transform">استكشف الأثر</span>
              <ArrowDown className="w-5 h-5 text-[var(--brand-gold-light)] animate-bounce" />
            </button>
          </div>

        </div>
      </div>
    </section>
