    <section className="relative pt-32 lg:pt-48 pb-0 overflow-hidden bg-gradient-to-b from-[#02110a] via-[#052317] to-[var(--background)]" style={{ direction: "rtl" }}>
      
      {/* Cinematic Topography & Lighting */}
      <div className="absolute inset-0 pattern-bg-gold opacity-[0.03] pointer-events-none z-0 mix-blend-overlay" />
      <div className="absolute top-0 right-0 w-[80%] h-[70%] bg-gradient-to-bl from-[var(--brand-green)]/30 via-emerald-900/10 to-transparent blur-[120px] rounded-full pointer-events-none z-0" />
      <div className="absolute bottom-0 left-0 w-[60%] h-[60%] bg-gradient-to-tr from-[var(--brand-gold)]/10 to-transparent blur-[150px] rounded-full pointer-events-none z-0" />

      {/* Content wrapper */}
      <div className="relative z-10 w-full mb-16 lg:mb-24">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          
          <div className="grid lg:grid-cols-12 gap-12 lg:gap-16 items-center">
            
            {/* ───── Right Column (Arabic layout, visual weight leading) ───── */}
            <div className="lg:col-span-7 xl:col-span-7 text-right relative z-20">
              <motion.div
                initial={{ opacity: 0, y: 30 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.8, ease: [0.16, 1, 0.3, 1] }}
              >
                {/* Micro-credential trust badge */}
                <div className="inline-flex items-center gap-3 bg-white/5 backdrop-blur-xl border border-white/10 rounded-full px-5 py-2.5 mb-8 shadow-sm">
                  <Shield className="w-4.5 h-4.5 text-[var(--brand-gold)] shrink-0" />
                  <span className="text-white/90 text-[0.7rem] sm:text-xs font-semibold leading-normal font-readex tracking-wide">
                    إشراف استشاري وتنموي • حوكمة معتمدة وإجراءات رقابة ميدانية
                  </span>
                </div>

                {/* Singular, powerful dominant headline emphasizing human dignity and long-term impact */}
                <h1 className="text-white mb-6 font-alexandria font-black tracking-tight drop-shadow-2xl" style={{ fontSize: "clamp(2.25rem, 4.5vw, 4rem)", lineHeight: 1.25 }}>
                  نصنع <span className="text-[var(--brand-gold)] font-extrabold relative inline-block">
                    التنمية المستدامة
                  </span>
                  <br />
                  ونحفظ <span className="text-white font-extrabold">الكرامة الإنسانية</span>
                </h1>

                {/* Highly communicative, professional subtitle avoiding generic slogans */}
                <p className="text-white/75 mb-10 lg:mb-14 max-w-2xl font-readex leading-relaxed" style={{ fontSize: "clamp(1rem, 1.2vw, 1.15rem)", fontWeight: 300 }}>
                  مؤسسة وطنية رائدة متخصصة في هندسة الأثر وتطبيق حلول التنمية المستدامة باليمن. نتبنى نماذج حوكمة مرنة في إدارة المساعدات وإطلاق مشاريع السقيا النظيفة، التمكين الاقتصادي للأسر المنتجة، والتدخلات الإغاثية المستندة إلى المسوح الميدانية الديموغرافية والتوثيق الرقمي الكامل.
                </p>

                {/* True Luxury CTAs */}
                <div className="flex flex-col sm:flex-row items-stretch sm:items-center justify-start gap-4 mb-14 lg:mb-20">
                  <motion.button
                    onClick={() => setCurrentPage("donate")}
                    className="w-full sm:w-auto group relative flex items-center justify-center gap-3 px-8 py-4 bg-[var(--brand-gold)] text-[#02110a] rounded-xl font-extrabold shadow-[0_8px_32px_rgba(212,175,55,0.3)] hover:bg-[#E5C158] hover:shadow-[0_16px_48px_rgba(212,175,55,0.4)] transition-all duration-300 text-base lg:text-lg focus:outline-none focus:ring-2 focus:ring-[var(--brand-gold)] focus:ring-offset-4 focus:ring-offset-[#052317] font-readex overflow-hidden"
                    whileHover={{ y: -2 }}
                    whileTap={{ scale: 0.98 }}
                  >
                    <Heart className="relative z-10 w-5 h-5 text-[#02110a] fill-[#02110a]" />
                    <span className="relative z-10">المساهمة وبوابة الزكاة</span>
                  </motion.button>

                  <motion.button
                    onClick={() => setCurrentPage("programs")}
                    className="w-full sm:w-auto group flex items-center justify-center gap-3 px-8 py-4 bg-transparent text-white border-2 border-white/20 rounded-xl hover:bg-white/5 hover:border-white/40 transition-all duration-300 font-semibold text-sm lg:text-base focus:outline-none focus:ring-2 focus:ring-white/50 focus:ring-offset-4 focus:ring-offset-[#052317] font-readex relative overflow-hidden"
                    whileHover={{ y: -2 }}
                    whileTap={{ scale: 0.98 }}
                  >
                    <span className="relative z-10">مصفوفة البرامج التنموية</span>
                  </motion.button>
                </div>

                {/* Highly detailed Trust Metric Blocks instead of generic stat grids */}
                <div className="border-t border-white/10 pt-10 max-w-2xl">
                  <div className="grid grid-cols-3 gap-4 md:gap-6">
                    {[
                      { value: formatStat(metrics?.totalBeneficiaries), label: "مستفيد ميداني موثق", icon: Users },
                      { value: formatStat(metrics?.activeProjects), label: "مشروع تنموي مستدام", icon: Star },
                      { value: formatStat(metrics?.totalPartners), label: "مانح وشريك استراتيجي", icon: Handshake },
                    ].map((stat, idx) => {
                      const Icon = stat.icon;
                      return (
                        <div key={idx} className="text-right">
                          <div className="flex items-center gap-2 mb-2">
                            <Icon className="w-4 h-4 text-[var(--brand-gold)] shrink-0 opacity-100" />
                            <span className="text-[0.65rem] sm:text-xs font-semibold text-white/70 font-readex">{stat.label}</span>
                          </div>
                          <div className="text-white font-black font-alexandria text-xl md:text-3xl tracking-tighter drop-shadow-lg">
                            {stat.value}
                          </div>
                        </div>
                      );
                    })}
                  </div>
                </div>
              </motion.div>
            </div>

            {/* ───── Left Column (Majestic Spiritual Covenant and Trust Panel) ───── */}
            <div className="lg:col-span-5 xl:col-span-5 relative z-30">
              <motion.div
                initial={{ opacity: 0, scale: 0.95 }}
                animate={{ opacity: 1, scale: 1 }}
                transition={{ duration: 0.8, delay: 0.2, ease: [0.16, 1, 0.3, 1] }}
                onMouseEnter={() => setIsPaused(true)}
                onMouseLeave={() => setIsPaused(false)}
                className="relative rounded-3xl p-8 lg:p-12 overflow-hidden bg-[#0A1A13]/60 backdrop-blur-3xl border border-white/10 shadow-[0_30px_100px_rgba(0,0,0,0.6)] min-h-[440px] flex flex-col justify-between group"
              >
                {/* Subtle structural edge highlight */}
                <div className="absolute inset-x-0 top-0 h-[1px] bg-gradient-to-r from-transparent via-white/20 to-transparent" />
                <div className="absolute inset-y-0 right-0 w-[1px] bg-gradient-to-b from-transparent via-white/10 to-transparent" />

                {/* Section B: Spiritual Guidance (Rotating Quranic Ayah/Hadith Widget) */}
                <div className="relative h-full flex flex-col justify-between flex-1">
                  <div>
                    <div className="flex flex-wrap items-center justify-between gap-4 mb-10 pb-6 border-b border-white/5">
                      <span className="px-4 py-1.5 rounded-full bg-[var(--brand-gold)]/10 border border-[var(--brand-gold)]/20 text-[var(--brand-gold)] text-xs font-bold flex items-center gap-2 font-readex">
                        <Sparkles className="w-3.5 h-3.5" />
                        {current.type === 'ayah' ? 'تذكير بالآية الكريمة' : 'تذكير بالحديث الشريف'}
                      </span>
                      <span className="text-white/50 text-xs font-medium font-readex tracking-wider">{current.reference}</span>
                    </div>

                    <div className="min-h-[180px] flex items-center mb-6 overflow-hidden">
                      <AnimatePresence mode="wait">
                        <motion.div
                          key={verseIndex}
                          initial={{ opacity: 0, filter: "blur(4px)" }}
                          animate={{ opacity: 1, filter: "blur(0px)" }}
                          exit={{ opacity: 0, filter: "blur(4px)" }}
                          transition={{ duration: 0.6, ease: "easeInOut" }}
                          className="w-full"
                        >
                          <p className="text-white font-medium text-xl sm:text-2xl lg:text-[1.75rem] leading-relaxed text-right font-serif tracking-wide" style={{
                            fontFamily: "'Noto Naskh Arabic', 'Traditional Arabic', serif",
                            lineHeight: 2.2,
                            textShadow: "0 10px 30px rgba(0,0,0,0.5)"
                          }}>
                            {current.arabic}
                          </p>
                        </motion.div>
                      </AnimatePresence>
                    </div>
                  </div>

                  <div className="mt-8 pt-6 border-t border-white/5 flex flex-col sm:flex-row items-center justify-between gap-6">
                    {/* Manual control slider dots with larger touch targets for accessibility */}
                    <div className="flex gap-2" dir="ltr" role="tablist">
                      {ISLAMIC_TEXTS.map((_, i) => (
                        <button
                          key={i}
                          role="tab"
                          aria-selected={i === verseIndex}
                          aria-label={`تغيير النص ${i + 1}`}
                          onClick={() => setVerseIndex(i)}
                          className="relative group p-2 -m-2 focus:outline-none"
                        >
                          <span className={`block h-1.5 rounded-full transition-all duration-500 ease-out ${i === verseIndex ? 'w-8 bg-[var(--brand-gold)]' : 'w-2 bg-white/20 group-hover:bg-white/40 group-focus:ring-2 ring-white/50'}`} />
                        </button>
                      ))}
                    </div>
                    
                    <span className="text-white/30 text-[10px] sm:text-xs font-readex tracking-widest uppercase">مؤسسة رحماء بينهم للأعمال الإنسانية</span>
                  </div>
                </div>
              </motion.div>
            </div>
          </div>
        </div>
      </div>

      {/* Majestic Video Section - World Class Framing & Spacing */}
      <div className="w-full relative z-20" style={{ direction: "rtl" }}>
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative">
          <div className="w-full rounded-2xl lg:rounded-[2.5rem] overflow-hidden shadow-[0_40px_100px_rgba(0,0,0,0.25)] border-4 lg:border-[8px] border-white/20 bg-black aspect-video md:aspect-[21/9]">
             <HeroVideoBlock />
          </div>

          {/* Breathing room after video and scroll indicator */}
          <div className="mt-16 flex justify-center pb-12">
            <button 
              type="button"
              className="group flex flex-col items-center gap-3 text-emerald-950/40 hover:text-[var(--brand-green)] transition-all focus:outline-none focus:ring-2 focus:ring-[var(--brand-green)]/30 rounded-lg p-2" 
              onClick={() => {
                const target = document.getElementById('quick-donation');
                if (target) target.scrollIntoView({ behavior: 'smooth' });
              }}
            >
              <span className="text-[0.65rem] tracking-wider uppercase font-bold font-readex group-hover:-translate-y-1 transition-transform">استكشف الأثر</span>
              <ArrowDown className="w-5 h-5 text-[var(--brand-gold)] animate-bounce" />
            </button>
          </div>
        </div>
      </div>
    </section>
